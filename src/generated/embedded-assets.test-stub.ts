// @ts-nocheck
// Test-only stub for @easyresearch/embedded-assets: keeps vitest from
// loading the generated module (whose `with { type: "file" }` imports would
// execute real web bundles in the node test environment).
export const embeddedFiles: Record<string, string> = {};
export const embeddedVersion = "0.0.0-dev";
